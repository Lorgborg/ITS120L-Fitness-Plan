import React from 'react';
import PropTypes from 'prop-types';
import { mongoWrapper } from './mongo.styled';

const mongo = () => (
 <mongoWrapper>
    mongo Component
 </mongoWrapper>
);

mongo.propTypes = {};

mongo.defaultProps = {};

export default mongo;
